import { createFileRoute, Link } from "@tanstack/react-router";
import { CNH_LOGO } from "@/lib/companies";
import { COMPANIES } from "@/lib/companies";

export const Route = createFileRoute("/")({
  head: () => ({
    meta: [
      { title: "NexDocs — AI Business Documents for South Africa" },
      { name: "description", content: "Generate professional South African business documents in seconds with AI. 10-day free trial, then R99/month. Contracts, quotes, HR & legal forms." },
      { property: "og:title", content: "NexDocs — AI Business Documents for South Africa" },
      { property: "og:description", content: "Contracts, quotes, HR letters, legal forms — generated by AI for SA businesses." },
      { property: "og:url", content: "https://nexdoc-cossanexusholdings.lovable.app/" },
    ],
    links: [{ rel: "canonical", href: "https://nexdoc-cossanexusholdings.lovable.app/" }],
  }),
  component: Landing,
});

function Landing() {
  return (
    <div className="mx-auto max-w-6xl px-4">
      <section className="py-14 sm:py-24 text-center">
        <img src={CNH_LOGO} alt="NexDocs AI" className="mx-auto h-24 w-24 rounded-2xl object-cover shadow-2xl shadow-black/40" />
        <div className="mt-6 text-xs uppercase tracking-[0.2em] text-gold">NexDocs AI</div>
        <h1 className="mt-3 font-display text-4xl sm:text-6xl leading-tight">
          <span className="text-gold-gradient">South Africa's</span>
          <br />
          <span className="text-foreground">AI Business Platform</span>
        </h1>
        <p className="mx-auto mt-5 max-w-xl text-base sm:text-lg text-muted-foreground">
          Draft contracts, quotes, invoices, HR letters and POPIA policies in seconds — plus a ChatGPT-style AI assistant trained for South African business, law and compliance.
        </p>
        <div className="mt-8 flex flex-col sm:flex-row gap-3 justify-center">
          <Link to="/auth" className="rounded-lg bg-gold-gradient px-6 py-3 font-semibold text-primary-foreground shadow-lg">
            Start 10-day free trial
          </Link>
          <Link to="/assistant" className="rounded-lg border border-gold/40 px-6 py-3 font-semibold text-foreground">
            Try the AI Assistant
          </Link>
        </div>
        <p className="mt-4 text-xs text-muted-foreground">Then just R99/month. No credit card required. Cancel anytime.</p>
      </section>

      <section className="pb-20">
        <h2 className="font-display text-2xl sm:text-3xl text-center text-foreground mb-8">
          Six companies. <span className="text-gold-gradient">Forty-eight documents.</span>
        </h2>
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
          {COMPANIES.map((c) => (
            <div key={c.id} className="rounded-xl border border-border/60 bg-card/60 p-5 backdrop-blur">
              <div className="flex items-center gap-3">
                {c.logo ? (
                  <img src={c.logo} alt="" className="h-12 w-12 rounded-md object-cover" />
                ) : (
                  <div className="h-12 w-12 rounded-md bg-gold-gradient flex items-center justify-center font-display text-xl text-primary-foreground">SA</div>
                )}
                <div>
                  <div className="font-display text-lg text-foreground">{c.name}</div>
                  <div className="text-xs text-muted-foreground">{c.tagline}</div>
                </div>
              </div>
              <div className="mt-3 text-sm text-gold">{c.documents.length} documents</div>
            </div>
          ))}
        </div>
      </section>
    </div>
  );
}
